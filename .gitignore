# Secrets — never commit these
.env
token.json
credentials.json
secrets/

# State files — state.json contains X auth cookies, never commit
state.json
# sent_digests.json is committed so GitHub Actions can persist dedup state

# X scraper outputs
bookmarks.csv
likes.csv
debug_timeout.png

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# Caches
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Logs
*.log
